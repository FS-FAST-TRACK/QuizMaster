import LogoutButton from "./logout-button";

const Nav = () => {
    return (
        <nav className="flex h-10 px-10 bg-black">
            <LogoutButton />
        </nav>
    );
};

export default Nav;
